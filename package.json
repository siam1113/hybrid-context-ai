{
  "name": "hybrid-context-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Hybrid Context-Compiled AI QA platform for application operational intelligence.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "check": "npm run build && npm test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
